// Removed: tests for the manual attendance tab, which was superseded by
// Alvin's participants view. Safe to delete this file along with
// src/EventAttendanceTab.tsx once you have a moment.
import { describe, it } from "vitest"

describe.skip("EventAttendanceTab (removed)", () => {
  it("was superseded by the participants view", () => {})
})
